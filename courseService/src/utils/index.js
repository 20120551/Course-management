const {
    GenerateSalt, 
    GeneratePassword,
    ValidatePassword,
    GenerateSignature,
    GenerateToken,
    VerifyToken,
    FormatData,
    ValidateSignature,
} = require('./handleData');

module.exports = {
    GenerateSalt, 
    GeneratePassword,
    ValidatePassword,
    GenerateSignature,
    GenerateToken,
    VerifyToken,
    FormatData,
    ValidateSignature,
}