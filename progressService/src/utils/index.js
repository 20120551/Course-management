const {
    GenerateSalt, 
    GeneratePassword,
    ValidatePassword,
    GenerateSignature,
    GenerateToken,
    VerifyToken,
    FormatData,
    ValidateSignature,
    exportPDF
} = require('./handleData');

module.exports = {
    GenerateSalt, 
    GeneratePassword,
    ValidatePassword,
    GenerateSignature,
    GenerateToken,
    VerifyToken,
    FormatData,
    ValidateSignature,
    exportPDF
}